# Python
__pycache__/
*.pyc

# sistema operacional
Thumbs.db
desktop.ini
.DS_Store

# projetos salvos pelo usuário na própria pasta
projeto-*.json

# saida das provas (arquivos de teste regenerados a cada execucao)
ferramentas/_temp/

# planilha de controle da SEINFRA: fonte do catalogo, fica so na maquina
referencia/

# canal de trabalho entre agentes: registro interno, nao e o produto
_canal/

# arquivos de trabalho dos agentes (rascunho, prefixo _)
ferramentas/_*

# Material do escritório, referência de construção — não é nosso para publicar.
# A planilha viva da obra, o relatório do Power BI e o projeto gerado a partir deles ficam
# só na máquina. Mesmo critério de `referencia/`: o repositório vai para o GitHub Pages, e
# planilha interna de contrato não vai junto por descuido.
11-CONTROLE AM-010/
referencia/
